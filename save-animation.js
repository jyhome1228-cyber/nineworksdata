(()=>{if(window.__nineworksSaveMotion)return;window.__nineworksSaveMotion=true;const wait=ms=>new Promise(r=>setTimeout(r,ms));const hex=()=>Array.from({length:11},()=>Math.floor(Math.random()*16).toString(16).toUpperCase()).join('');async function run(button,state){if(button.dataset.processing==='1')return;button.dataset.processing='1';button.disabled=true;button.classList.remove('is-saved');button.classList.add('is-processing');button.textContent='PROCESSING';state.setAttribute('aria-live','polite');state.classList.add('save-sequence');const reduced=window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;const steps=[['> ENCODING NOTE',150],['> WRITE LOG',180],['> INDEX PROJECT',190],['> VERIFY DATA',180]];if(reduced){await wait(80)}else{for(const [label,duration] of steps){const started=performance.now();while(performance.now()-started<duration){state.innerHTML=`<span class="save-op">${label}</span><span class="save-code">${hex()}</span>`;await wait(42)}}}const now=new Intl.DateTimeFormat('en-GB',{hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date());state.classList.remove('save-sequence');state.innerHTML=`<span class="save-done"><i>✓</i>SAVED</span><span class="save-time">${now}</span>`;button.classList.remove('is-processing');button.classList.add('is-saved');button.textContent='SAVED';await wait(reduced?250:650);button.classList.remove('is-saved');button.textContent='SAVE NOTE';button.disabled=false;delete button.dataset.processing}document.addEventListener('click',event=>{const button=event.target.closest('#saveNote');if(!button||button.dataset.processing==='1')return;const state=document.querySelector('#saveState');if(!state)return;queueMicrotask(()=>run(button,state))})})();